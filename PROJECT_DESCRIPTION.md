# 项目详细说明

## 1. 项目概览

本项目是一个基于 Ultralytics YOLO 训练栈的目标检测项目，当前训练目标已经调整为只定位商品包装图中的 `nutrition_table`（营养成分表）区域。

从仓库中的样例图像、类别定义和标注内容可以看出，项目场景主要围绕中文商品包装背标或详情页图像，关注的是密集文本区域的结构化定位，而不是整张图像的分类或 OCR 文本识别。

项目当前已经具备完整的最小闭环：

1. 原始数据存放在 `data/`
2. 通过 `split_dataset.py` 切分为训练/验证/测试集并生成 `dataset/dataset.yaml`
3. 通过 `train.py` 完成模型训练、进度展示和 TensorFlow 日志写入
4. 通过 `export_onnx.py` 将最佳权重导出为 ONNX，便于后续部署

仓库中还保留了训练产物，包括权重文件、曲线图、混淆矩阵、可视化样例和 ONNX 模型，说明该项目已经跑过至少一轮完整训练。

## 2. 业务目标与适用场景

该项目解决的问题不是“识别商品是什么”，而是“在商品包装图中把需要的信息块先找出来”。这类能力通常可以作为上游视觉模块，为后续能力提供输入，例如：

- OCR 识别前的区域裁剪
- 食品标签合规检查
- 商品信息抽取
- 营养信息自动录入
- 电商详情页或包装图的文本结构分析

从样例图像观察，营养成分表区域通常具有以下特点：

- 文本密集
- 形状接近矩形，但在真实图像中存在透视、倾斜和拍摄角度变化
- 同一张图中原始标注里常同时出现营养成分表和产品信息块，但当前训练只保留营养成分表
- 背景可能复杂，存在包装纹理、商品图案和其他干扰文字

这解释了为什么数据标注不是简单的轴对齐矩形框，而是多边形轮廓标注：对于倾斜文本块或透视变化明显的区域，多边形比普通矩形更贴近真实边界。

## 3. 仓库结构说明

当前仓库的核心结构如下：

```text
train_yolo26s/
├─ data/                       # 原始数据
│  ├─ images/                  # 原始图片
│  ├─ labels/                  # 原始标注
│  ├─ classes.txt              # 类别定义
│  └─ notes.json               # 标注元信息（来自 Label Studio）
├─ dataset/                    # 切分后的训练数据集
│  ├─ images/train|val|test
│  ├─ labels/train|val|test
│  ├─ dataset.yaml             # 训练使用的数据配置
│  └─ *.cache                  # Ultralytics 生成的缓存
├─ runs/                       # 训练过程与结果产物
│  ├─ detect/runs/train/yolo26s-vehicle-opt/
│  └─ tf_monitor/yolo26s-vehicle-opt/
├─ split_dataset.py            # 数据切分脚本
├─ train.py                    # 训练脚本
├─ export_onnx.py              # ONNX 导出脚本
├─ requirements.txt            # 依赖列表
├─ yolo26n.pt                  # 备用预训练权重
└─ yolo26s.pt                  # 当前训练使用的预训练权重
```

## 4. 数据集说明

### 4.1 原始数据来源与元信息

`data/notes.json` 显示该数据集带有以下元信息：

- `year`: 2026
- `version`: 1.0
- `contributor`: `Label Studio`

说明这批标注数据很可能由 Label Studio 导出并整理后放入仓库。

`data/classes.txt` 仍保存原始标注的两个类别：

```text
nutrition_table
product_info_block
```

当前训练数据集不是直接使用原始双类标签，而是由 `split_dataset.py` 在切分时过滤得到单类训练集：

- 保留类别：`nutrition_table`
- 过滤类别：`product_info_block`
- 仅含 `product_info_block` 的图片保留为空标注，作为负样本参与训练

### 4.2 数据规模

基于当前仓库统计结果：

| 项目 | 数量 |
| --- | ---: |
| 原始图片数 `data/images` | 214 |
| 原始标注数 `data/labels` | 214 |
| 训练集图片数 | 149 |
| 验证集图片数 | 42 |
| 测试集图片数 | 23 |

切分比例与 `split_dataset.py` 中定义一致：

- 训练集：70%
- 验证集：20%
- 测试集：10%

### 4.3 类别分布

原始标注与当前训练集的类别统计需要分开看。

#### 原始标注统计

按原始标注文件统计，全量数据共有 439 个实例：

| 类别 ID | 类别名 | 实例数 |
| --- | --- | ---: |
| 0 | `nutrition_table` | 219 |
| 1 | `product_info_block` | 220 |

类别分布非常均衡，几乎是 1:1。

图像级分布如下：

- 同时包含两类目标的图像：201 张
- 仅包含 `nutrition_table` 的图像：5 张
- 仅包含 `product_info_block` 的图像：8 张

这说明原始数据里大多数图片都会同时出现两类区域。

#### 当前单类训练集统计

按重新生成后的训练集统计，当前 `dataset/` 中只保留 `nutrition_table`：

| 子集 | `nutrition_table` |
| --- | ---: |
| train | 153 |
| val | 43 |
| test | 23 |

补充说明：

- 当前训练集总实例数为 `219`
- 当前训练集总标签文件数为 `214`
- 其中空标注文件 `8` 个，对应原本只包含 `product_info_block` 的图片

### 4.4 标注格式

这是项目里一个非常重要的实现细节。

标注文件并不是常见的 YOLO 检测五列格式：

```text
class x_center y_center width height
```

而是**多边形轮廓格式**，即：

```text
class x1 y1 x2 y2 x3 y3 ...
```

实际统计结果显示：

| 多边形顶点数 | 实例数 |
| --- | ---: |
| 4 点 | 431 |
| 5 点 | 4 |
| 6 点 | 4 |

因此更准确地说，当前数据是“以多边形形式标注文本区域”的数据集，其中大多数目标是四边形，少量是五边形或六边形。

这意味着：

- 标注比普通矩形框更贴合目标边界
- 更适合有透视畸变或斜拍的文本区域
- 当前训练脚本运行的是 `detect` 任务，且当前训练只学习 `nutrition_table` 这一类轮廓目标

最后这一点需要特别留意：**从仓库事实看，数据格式与训练任务类型存在一定语义差异**。当前训练结果说明该流程是可运行的，但后续如果继续演进项目，应明确确认是：

- 继续把轮廓数据视为检测框来源来训练检测模型
- 还是转为更严格的分割/旋转框任务

### 4.5 图像规格

原始图像全部为 `.jpg` 格式，共 214 张。尺寸统计如下：

| 维度 | 最小值 | 最大值 | 平均值 |
| --- | ---: | ---: | ---: |
| 宽度 | 424 | 5824 | 2281.70 |
| 高度 | 430 | 5824 | 2416.39 |

这说明图像来源较杂，分辨率跨度很大，包含手机拍摄图和较高分辨率的详情图/裁切图。训练时统一缩放到 `640`，有助于控制显存和训练速度，但也意味着细粒度小文本会发生一定信息压缩。

### 4.6 每图目标数

按原始标注文件统计：

- 平均每张图包含 `2.05` 个目标
- 最少每张图 `1` 个目标
- 最多每张图 `8` 个目标
- 原始标注中没有空标注文件

这与业务场景一致：多数图像有 1 到 3 个结构化文本区域，少数详情长图会包含更多重复布局。

## 5. 核心脚本职责

### 5.1 `split_dataset.py`

该脚本负责把 `data/` 下的原始数据切分为训练集、验证集和测试集，并生成 Ultralytics 所需的数据配置文件。

核心流程如下：

1. 从 `data/classes.txt` 读取原始类别名称
2. 遍历 `data/images`，检查每张图是否存在同名 `.txt` 标注
3. 使用固定随机种子 `42` 对图像-标注对进行打乱
4. 按 `0.7 / 0.2 / 0.1` 划分为 train / val / test
5. 删除并重建整个 `dataset/` 目录
6. 仅保留 `nutrition_table` 标签并写入目标子目录
7. 对过滤后无目标的图片保留空标注文件
8. 生成单类的 `dataset/dataset.yaml`

这个脚本的优点是简单直接、可复现；代价是它会直接重建 `dataset/`，因此属于有明确副作用的预处理步骤。

### 5.2 `train.py`

这是项目的训练主脚本，主要做了四件事：

1. 加载预训练模型 `yolo26s.pt`
2. 解析数据配置和训练设备
3. 注册训练过程回调，用于：
   - 写 TensorFlow 标量日志
   - 通过 `tqdm` 显示 epoch 进度
4. 调用 `model.train(...)` 执行 Ultralytics 训练

除了标准训练调用外，这个脚本额外做了几个工程化处理：

- 数据配置文件自动回退
- CUDA 不可用时自动回退到 CPU
- 将训练损失和验证指标写入 `runs/tf_monitor/`
- 使用 `tqdm` 展示 epoch 级进度条

### 5.3 `export_onnx.py`

该脚本把训练得到的最佳权重导出为 ONNX：

- 默认输入权重：`runs/detect/runs/train/yolo26s-vehicle-opt/weights/best.pt`
- 默认导出尺寸：`640`
- `opset`: `13`
- `dynamic`: `False`
- `simplify`: `False`
- `half`: `False`

脚本会自动选择 GPU 或 CPU 导出，但如果手动把 `HALF=True`，则必须使用 GPU。

## 6. 训练配置分析

当前训练脚本的关键参数可从 `train.py` 确认：

| 参数 | 当前值 |
| --- | --- |
| 模型权重 | `yolo26s.pt` |
| 任务类型 | `detect` |
| 数据配置 | `dataset/dataset.yaml` |
| 当前训练类别 | `nutrition_table` |
| 目标输入尺寸 | `640` |
| 计划训练轮数 | `180` |
| 批大小 | `-1`（AutoBatch） |
| 设备 | `0`（优先 GPU） |
| worker 数 | `4` |
| 优化器 | `auto` |
| 学习率策略 | `cos_lr=True` |
| 早停耐心值 | `30` |
| Mosaic 关闭时机 | 最后 10 轮 |
| 缓存 | `cache=True` |
| 周期保存 | 每 10 轮保存一次 |

数据增强参数也做了明确配置：

| 参数 | 值 |
| --- | ---: |
| `hsv_h` | 0.012 |
| `hsv_s` | 0.5 |
| `hsv_v` | 0.3 |
| `degrees` | 3.0 |
| `translate` | 0.05 |
| `scale` | 0.3 |
| `shear` | 0.0 |
| `flipud` | 0.0 |
| `fliplr` | 0.2 |

这组增强配置总体偏保守，符合该场景特点：

- 允许轻微颜色和亮度扰动
- 允许小幅旋转、平移和缩放
- 避免过强几何变换破坏文本结构
- 只保留少量水平翻转，不做垂直翻转

对于中文包装文本区域来说，这种配置比强增强更稳妥。

## 7. 训练过程与已有结果

### 7.1 历史训练运行状态

仓库中当前保存的 `results.csv`、曲线图和权重文件，来自本次单类改造之前的历史训练结果。虽然脚本计划训练 `180` 轮，但历史 `results.csv` 只记录到了 `epoch 117`。结合 `patience=30` 可以推断，当时训练大概率在提前停止条件触发后结束，而不是完整跑满 180 轮。

### 7.2 历史关键指标

从当前训练结果中提取的两个关键状态如下：

| 状态 | epoch | Precision | Recall | mAP50 | mAP50-95 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 当前最后一轮 | 117 | 0.87216 | 0.75188 | 0.81082 | 0.61404 |
| 最佳 `mAP50-95` | 87 | 0.78998 | 0.81638 | 0.83653 | 0.62669 |

这些指标反映的是旧版双类训练结果，不代表当前单类配置下的最终效果。它们只能说明该仓库此前已经具备可运行的训练闭环；若要评估当前单类任务，需要重新训练并生成新的指标文件。

### 7.3 已生成产物

当前仓库中已经保存了以下训练结果：

- `best.pt`
- `last.pt`
- `best.onnx`
- `epoch0.pt` 到 `epoch110.pt` 的周期快照
- `results.csv`
- `results.png`
- `confusion_matrix.png`
- `confusion_matrix_normalized.png`
- `BoxP_curve.png`
- `BoxR_curve.png`
- `BoxPR_curve.png`
- `BoxF1_curve.png`
- 训练批次与验证批次的可视化图片
- TensorFlow 日志文件：`runs/tf_monitor/yolo26s-vehicle-opt/`

权重文件大小也能反映出项目当前的模型状态：

| 文件 | 大小 |
| --- | ---: |
| `best.pt` | 20.31 MB |
| `last.pt` | 20.31 MB |
| `best.onnx` | 38.12 MB |

## 8. 实现上的关键细节与注意事项

### 8.1 数据配置文件默认路径已修正

当前 `train.py` 的 `main()` 已直接使用：

```python
data_yaml = "./dataset/dataset.yaml"
```

同时脚本仍保留回退逻辑：如果传入路径不存在，会自动回退到 `./dataset/dataset.yaml`。

### 8.2 训练输出目录比较特殊

`train.py` 中设置了：

```python
project="runs/train"
```

但实际保存目录是：

```text
runs/detect/runs/train/yolo26s-vehicle-opt
```

说明 Ultralytics 在当前版本/当前模型任务下仍然套了一层 `detect` 目录。这个路径并不直观，后续如果要做自动化脚本、部署流水线或文档说明，建议统一梳理输出目录规则。

### 8.3 `split_dataset.py` 会重建整个 `dataset/`

脚本执行时会调用 `shutil.rmtree(output_dir)` 删除原 `dataset/`。因此：

- 该脚本不适合在 `dataset/` 中混放手工文件
- 每次运行都会覆盖切分结果
- 如果以后需要保留多套切分，建议增加版本化输出目录
- 当前版本还会在重建过程中把训练标签过滤为单类

### 8.4 `export_onnx.py` 对权重路径是硬编码的

当前默认导出权重为：

```text
runs/detect/runs/train/yolo26s-vehicle-opt/weights/best.pt
```

如果后续切换实验名、模型名或输出路径，这个脚本需要同步修改，否则会直接找不到文件。

### 8.5 标注格式与任务类型需要后续明确

从实际标签可见，当前数据是多边形轮廓标注；而训练任务配置为 `detect`。这并不一定错误，但需要在项目定位上说清楚：

- 如果目标是“检测文本区域”，那当前流程可以理解为使用轮廓标注训练检测器
- 如果目标是“高精度拟合文本区域轮廓”，那未来可能更适合分割或旋转框任务

这一点不会阻碍当前项目运行，但会影响后续技术路线。

## 9. 推荐的使用流程

如果需要复现当前项目，建议按以下顺序执行：

### 9.1 安装依赖

```bash
conda activate train_env
pip install -r requirements.txt
```

其中 PyTorch 需要根据机器 CUDA 版本单独安装，`requirements.txt` 中已经给出 Windows + CUDA 12.6 的示例安装方式。

### 9.2 切分数据集

```bash
conda activate train_env
python split_dataset.py
```

执行后会得到新的 `dataset/` 和单类 `dataset/dataset.yaml`。

### 9.3 开始训练

```bash
conda activate train_env
python train.py
```

训练过程中：

- 控制台会显示 `tqdm` 进度
- `runs/tf_monitor/` 会写入 TensorFlow 标量日志
- `runs/detect/runs/train/yolo26s-vehicle-opt/` 会保存训练结果

### 9.4 导出 ONNX

```bash
conda activate train_env
python export_onnx.py
```

导出成功后可获得 `best.onnx`。

## 10. 当前项目状态总结

从仓库现状看，这不是一个空模板项目，而是一个已经完成过真实训练的定制化目标检测仓库。它具备以下特征：

- 当前训练目标清晰：定位商品图中的营养成分表区域
- 原始数据规模中等偏小：214 张图，2 个原始类别，439 个原始实例
- 当前训练数据规模：214 张图，1 个训练类别，219 个正样本实例，8 个空标注负样本
- 标注质量形式较好：多边形轮廓标注而非粗框
- 工程闭环完整：数据切分、训练、监控、导出都已具备
- 历史双类训练已有可用结果：最佳 `mAP50-95` 达到 `0.62669`

如果把它放在整个视觉系统中理解，这个仓库更像是“文本区域检测模块”的训练与导出工程，而不是完整的识别系统。后续若要继续扩展，最自然的方向通常会是：

- 增加数据量与场景多样性
- 统一标注格式与任务定义
- 在测试集上补充独立评估脚本
- 将 ONNX 模型接入部署或 OCR 流程

整体而言，当前项目已经具备较明确的业务价值、可运行的单类训练流程和可继续迭代的基础。
